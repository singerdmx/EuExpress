<div id='forum'>
  <h2><%= link_to t('forem.forum.forums'), root_path %> &raquo; <%= link_to forem_emojify(forum.category), [forum.category] %> &raquo; <%= link_to forem_emojify(forum.title), [forum] %></h2>
  <span id='description'><%= forem_format(forum.description) %></span>
  <div id='forum_topic_links' class='btn-group'>
    <% unless @topic.try(:new_record?) %>
      <% if can? :create_topic, @forum %>
        <%= link_to t('forem.topic.links.new'), new_forum_topic_path(forum), :class => "btn btn-primary", :id => "new-topic" %>
      <% end %>
    <% end %>
    <% if @topic %>
      <%= link_to t('forem.topic.links.back_to_topics'), forum_path(forum), :class => "btn" %>
    <% end %>
    <% if can? :moderate, @forum %>
      <%= link_to t('forem.forum.moderator_tools'), forum_moderator_tools_path(forum), :class => "btn btn-primary" %>
    <% end %>
  </div>
</div>
